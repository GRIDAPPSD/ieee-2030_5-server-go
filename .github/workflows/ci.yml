# #191: CI workflow scaffold (Plan 2 Phase 7 ticket 1 of 2).
# #192: CSIP harness integration + archive-grade logs (ticket 2 of 2).
# #195: workflow_dispatch entry point for re-attestation runs.
#
# Standard Go signal: build, vet, lint, test, test-race, coverage (`ci` job).
# CSIP V1.2 harness: build-tag matrix (csip_test_hooks off/on) plus race
# detector on the hooks-on suite. Per-job stdout/stderr is teed to a file
# and uploaded as a workflow artifact with 90-day retention so Phase 9
# #195 can index the archive-grade evidence.
#
# Re-attestation runs (#195): `gh workflow run ci.yml --ref <SHA>`.
# `workflow_dispatch` lets us pin a CI run to a specific main SHA for the
# conformance package without pushing a fresh commit. After the run
# completes, `scripts/archive-ci-logs.sh <run-id>` mirrors the artifacts
# into the Knowledge workspace at projects/ieee-2030_5-go/artifacts/
# outputs/ci-archive/.
#
# Runner: ubuntu-latest. This is a documented exception to the
# workspace `runs-on: self-hosted` policy; see .claude/rules/devops.md.
# Rationale: third-party-org repo (GRIDAPPSD), test-only CI (no deploy,
# no secrets, no docker push). Self-hosted is reserved for projects with
# deploy-bearing CI.
# Coverage gate is warn-not-fail at this stage; #193 (Phase 8) ratchets
# the gate to fail at the achieved threshold.
name: ci

on:
  # paths-ignore: doc-only changes do not affect build artifacts in this repo
  # (godoc is consumed externally, not embedded into a release artifact). See
  # .claude/rules/devops.md "Documentation-only changes should not trigger a
  # full CI rebuild"; invert this list if docs ever become part of the build.
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'CHANGELOG.md'
      - '.gitignore'
      - 'LICENSE'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'CHANGELOG.md'
      - '.gitignore'
      - 'LICENSE'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual run (e.g. "#194 re-attestation against SHA xyz")'
        required: false
        default: 'manual'

# Cancel in-flight runs for the same ref when a new push arrives.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: build / vet / lint / test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      # No GOPRIVATE here (dropped with the App-token mint): safe only
      # because this job builds from vendor/. GOPRIVATE also fed
      # GONOSUMDB, so a future non-vendor build needs it back or
      # github.com/GRIDAPPSD/* leaks to sum.golang.org.
      #
      # #214: Plan-4 decision 2: PR-gate runs property tests with a
      # pinned RAPID_SEED for deterministic, reproducible failures.
      # Reproduction: re-run with the same seed to get the same shrunk
      # counterexample. A nightly cron job with time-based seeding (broader
      # exploration) is a separate follow-up (out of #214 scope).
      # Seed is a non-zero integer; value 1747227600 derived from plan-4
      # approval timestamp 2026-05-14T15:00:00Z.
      RAPID_SEED: 1747227600
    steps:
      - name: Checkout
        # #219: bumped v4->v6 for Node.js 24 runtime.
        uses: actions/checkout@v7

      - name: Setup Go
        # #219: bumped v5->v6 for Node.js 24 runtime.
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Vet
        run: make vet

      - name: Build
        run: make build

      - name: Lint
        # golangci-lint v2.12.2 is built with go1.26.2 and supports modules
        # declaring `go 1.26.3` in go.mod. Earlier releases (through v2.9.0)
        # were compiled with go1.25 and reject go1.26 modules at startup.
        # Config schema is unchanged (v2). See `.golangci.yml`.
        # The action installs the binary (cached across runs) and runs it.
        # golangci-lint-action@v9 ships Node.js 24 (v9.0.0 release).
        # The `version:` input name is unchanged from v7.
        # #219: bumped v7->v9 for Node.js 24 runtime.
        # #252: bumped v2.9.0->v2.12.2 for go1.26 toolchain support.
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2

      - name: Supply the IEEE 2030.5 WADL
        id: wadl
        # The WADL is copyrighted by IEEE and is not in this repository (see
        # NOTICE). The reassembly, the digest verification, and the "write it
        # outside the checkout" rule moved verbatim into a composite action so
        # they have exactly one home; see .github/actions/supply-wadl/action.yml.
        # Behaviour on this workflow is unchanged.
        #
        # required: false here, and deliberately only here. This workflow runs
        # on pull_request, and GitHub withholds secrets from a fork's pull
        # request, so an outside contributor's run has to stay green with the
        # gated tests skipped. Triggers that always run in the base-repository
        # context pass required: true instead, so a deleted or half-rotated
        # secret fails them loudly rather than silently disarming the sweep;
        # core-freshness.yml is the worked example.
        uses: ./.github/actions/supply-wadl
        with:
          wadl-xz-b64: ${{ secrets.SEP2_WADL_XZ_B64 }}
          required: 'false'

      - name: Confirm the WADL gate is armed
        if: steps.wadl.outputs.armed == 'true'
        # SEP2_WADL_REQUIRED=1 already turns an absent or unreadable WADL into
        # a failure. This adds the positive assertion: TestWADLGateArmed has
        # to PASS, not SKIP and not vanish into "no tests to run", so a green
        # run cannot mean a conformance sweep that measured nothing against
        # the standard.
        run: |
          set -euo pipefail
          output=$(go test -run '^TestWADLGateArmed$' -v ./test/conformance/wadl/)
          echo "$output"
          case "$output" in
            *"--- PASS: TestWADLGateArmed"*)
              ;;
            *)
              echo "::error::A WADL was supplied but TestWADLGateArmed did not pass, so the conformance sweep measured nothing."
              exit 1
              ;;
          esac

      - name: Test
        # SEP2_WADL_PATH and SEP2_WADL_REQUIRED reach this step through
        # GITHUB_ENV from the WADL step above, when and only when the secret
        # was in scope.
        run: make test

      - name: Test (race detector)
        run: make test-race

      - name: Test (coverage)
        # Aggregate `./...` coverage profile for the upload artifact.
        # The fail-gate operates on the CSIP-scoped profile in the
        # `csip` job below (#193): this step stays informational because
        # raw ./... aggregates every first-party package (cmd/
        # entrypoints, test/stress tooling, handler packages whose
        # coverage comes only from the CSIP suite's cross-package
        # -coverpkg attribution and read 0% here), which drags the
        # aggregate well below policy and is not in the conformance
        # scope.
        run: make test-cover

      - name: Upload coverage artifact
        if: always()
        # #219: bumped v4->v7 for Node.js 24 runtime.
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            coverage.out
            coverage.html
          if-no-files-found: warn
          retention-days: 14

  # #192: CSIP V1.2 harness with the csip_test_hooks build-tag matrix.
  #
  # Two parallel jobs: tag `off` exercises the production code path, tag
  # `on` exercises the #27/#28 mutation + time-advance hooks. Both
  # must pass for the workflow to be green.
  #
  # The harness covers `./test/csip/...` and `./internal/...` so production
  # paths reached by CSIP tests participate in the captured signal. After
  # the matrix call, the hooks-on job runs the race detector against the
  # CSIP suite catches subscription-fan-out / aggregator races the unit
  # race pass might not surface.
  #
  # Archive-grade logs: each `go test` invocation tees stdout+stderr to a
  # per-step `.log` file via the Makefile-equivalent inline command, and
  # the per-job artifact ships at 90-day retention (GitHub max). Phase 9
  # #195 indexes these into the Knowledge repo and mirrors the bulk
  # tarball to the durable store it owns.
  csip:
    name: csip harness (hooks=${{ matrix.hooks }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: ci
    strategy:
      fail-fast: false
      matrix:
        hooks: [off, on]
    env:
      # No GOPRIVATE here either; same vendor-mode assumption and
      # GONOSUMDB caveat as the `ci` job's env block above.
      #
      # `SEP2_TEST_MUTATION_TOKEN` gates the #27 mutation surface in
      # hooks-on builds; without it, those endpoints stay disabled and the
      # binary logs a warning. The csip_test_hooks suite tolerates an
      # empty token (mutation-dependent tests skip), but CI exercises the
      # full path by providing a per-run token. Not a secret: the
      # mutation surface only exists in -tags csip_test_hooks builds and
      # only listens on the in-process test server.
      SEP2_TEST_MUTATION_TOKEN: ci-${{ github.run_id }}
    steps:
      - name: Checkout
        # #219: bumped v4->v6 for Node.js 24 runtime.
        uses: actions/checkout@v7

      - name: Setup Go
        # #219: bumped v5->v6 for Node.js 24 runtime.
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Prepare log directory
        run: mkdir -p ci-logs

      - name: Supply the SunSpec CSIP test PKI
        id: csip_pki
        # #348: the SunSpec-backed procedures (TestCSIPHandshakeWithSunSpecDeviceCert,
        # TestBASIC_001_DERIdentification) skip unless this material reaches the
        # runner, and nothing could previously demand it.
        #
        # The step runs on every trigger and the arming switch is passed IN, so
        # deleting or renaming the variable cannot skip the step silently: the
        # action annotates the disarmed state, and a secret present with the
        # switch off is a contradiction it fails on. `provisioned` and `required`
        # stay separate because "this repository has been provisioned" and "this
        # run may see the secret" are different questions.
        uses: ./.github/actions/supply-csip-pki
        with:
          pki-tar-xz-b64: ${{ secrets.CSIP_SUNSPEC_PKI_TAR_XZ_B64 }}
          pki-sha256: ${{ vars.CSIP_SUNSPEC_PKI_SHA256 }}
          provisioned: ${{ vars.CSIP_SUNSPEC_PKI_PROVISIONED }}
          # Derived from the trigger rather than hardcoded: false ONLY where
          # GitHub genuinely withholds secrets, which is a fork pull request.
          # Every other context reaches the base repository, so an absent secret
          # there is a deleted or half-rotated one and must fail. Hardcoding
          # 'false' cannot tell those two apart.
          required: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}

      - name: Confirm the CSIP fixture gate is armed
        if: steps.csip_pki.outputs.armed == 'true'
        # Conditional because the PKI is not provisioned yet, so a disarmed run is
        # still the expected state; the supply step annotates it. Drop this `if`
        # once the PKI is provisioned.
        #
        # CSIP_SUNSPEC_REQUIRED=1 already turns absent material into a failure.
        # This adds the positive assertion: TestCSIPFixtureGateArmed has to PASS,
        # not SKIP and not vanish into "no tests to run", so a green run cannot
        # mean the SunSpec-backed procedures measured nothing.
        shell: bash
        run: |
          set -euo pipefail
          status=0
          output=$(go test -run '^TestCSIPFixtureGateArmed$' -v ./test/csip/ 2>&1) || status=$?
          echo "$output"

          case "$output" in
            *"--- PASS: TestCSIPFixtureGateArmed"*) verdict=pass ;;
            *"--- FAIL: TestCSIPFixtureGateArmed"*) verdict=fail ;;
            *) verdict=none ;;
          esac

          if [ "$verdict" = pass ] && [ "$status" -eq 0 ]; then
            exit 0
          fi
          echo "::error::TestCSIPFixtureGateArmed reached verdict '${verdict}' with go test exiting ${status}. A PASS is the only outcome that means the SunSpec PKI was supplied and loads; a skip, a build failure, and a renamed test all look alike from the exit code."
          exit 1

      - name: CSIP harness (default tag)
        if: matrix.hooks == 'off'
        # `set -o pipefail` so `tee` does not mask test failures.
        # The Makefile `test-csip` target wraps `go test ./test/csip/...
        # ./internal/...` under the production build tag.
        shell: bash
        run: |
          set -o pipefail
          make test-csip 2>&1 | tee ci-logs/csip-tag-off.log

      - name: CSIP harness wrapper smoke (scripts/test-csip-server.sh)
        if: matrix.hooks == 'off'
        # Validates the CLAUDE.md-referenced `make test-csip-server` entry
        # point keeps working alongside the new make targets. The script
        # wraps `go test -v ./test/csip/...` (production tag) and skips
        # cleanly when SunSpec PKI fixtures are absent.
        shell: bash
        run: |
          set -o pipefail
          make test-csip-server 2>&1 | tee ci-logs/csip-server-script.log

      - name: CSIP harness (csip_test_hooks tag)
        if: matrix.hooks == 'on'
        shell: bash
        run: |
          set -o pipefail
          make test-csip-hooks 2>&1 | tee ci-logs/csip-tag-on.log

      - name: CSIP harness race detector (csip_test_hooks tag)
        if: matrix.hooks == 'on'
        # Race only runs on the hooks-on axis (per Phase 7 doc): the
        # mutation + time-advance hooks are the surface most likely to
        # surface subscription-fan-out / aggregator races.
        shell: bash
        run: |
          set -o pipefail
          make test-csip-race 2>&1 | tee ci-logs/csip-race.log

      - name: CSIP scoped coverage profile (csip_test_hooks tag)
        # #193 Deliverable 3: runs the CSIP suite once more under
        # the hooks-on axis with the scoped -coverpkg set so the gate
        # operates on production code reachable from CSIP-mode
        # execution. Achieved 79.1% at #192 merge; gate floors at
        # 78% (1pp below for measurement noise).
        if: matrix.hooks == 'on'
        shell: bash
        run: |
          set -o pipefail
          make test-csip-cover 2>&1 | tee ci-logs/csip-cover.log

      # Temporarily non-blocking: CSIP coverage is 77.1% vs the 80% gate
      # (pre-existing debt surfaced when GitHub CI was first enabled, tracked
      # as #301). Remove continue-on-error once CSIP coverage is back at >=80%.
      - name: CSIP coverage gate (fail-at-threshold)
        # #193: flips the gate from warn-not-fail (#191) to
        # fail at the achieved threshold. Unit-tested by
        # scripts/coverage_gate_test.go.
        continue-on-error: true
        if: matrix.hooks == 'on'
        shell: bash
        run: |
          set -o pipefail
          make coverage-gate 2>&1 | tee ci-logs/csip-coverage-gate.log

      - name: Upload CSIP archive-grade logs
        if: always()
        # 90-day retention = GitHub Actions max. Phase 9 #195 indexes
        # these artifacts into Knowledge and mirrors the bulk tarball to
        # the long-term durable store it owns.
        # #219: bumped v4->v7 for Node.js 24 runtime.
        uses: actions/upload-artifact@v7
        with:
          name: csip-logs-${{ matrix.hooks }}-${{ github.sha }}
          path: ci-logs/
          if-no-files-found: warn
          retention-days: 90

      - name: Upload CSIP scoped coverage profile
        # #193: separate artifact so Phase 9 #195 can pull the
        # scoped profile directly without parsing ci-logs/. Hooks-on
        # axis only (the axis that runs `make test-csip-cover`).
        # #219: bumped v4->v7 for Node.js 24 runtime.
        if: matrix.hooks == 'on' && always()
        uses: actions/upload-artifact@v7
        with:
          name: csip-coverage-${{ github.sha }}
          path: coverage-csip.out
          if-no-files-found: warn
          retention-days: 90

  ui-drift-check:
    name: Admin UI dist drift check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # No App token here: `make ui-check` runs npm and a local `git
    # status`, and never resolves a Go module, so this job needs no
    # module-access credential at all.
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        # Pinned to the exact version resolved by node-version: '24' in
        # run 33093474876 (2026-08-27), since ui-check compares built
        # bytes and a floating major can change output on a patch bump.
        uses: actions/setup-node@v7
        with:
          node-version: '24.19.0'
          cache: npm
          cache-dependency-path: internal/server/web/frontend/package-lock.json

      - name: Rebuild frontend and diff against committed dist/
        run: make ui-check

      - name: Run frontend component tests
        run: cd internal/server/web/frontend && npm run test

  gofmt:
    name: gofmt check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # No App token here: gofmt -l only reads and parses the local tree,
    # it never resolves a Go module or touches network.
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: Check formatting
        run: |
          # gofmt -l walks the whole tree; vendor/ holds go.mod-managed
          # copies of third-party source we do not author, so it is
          # excluded by path prefix (mirrors the Makefile gofmt-check
          # target). gofmt's own exit status (2 = a file it cannot parse)
          # is captured before that filtering, so a parse failure fails
          # this gate instead of grep -v silently swallowing it.
          out=$(gofmt -l . 2>&1) && status=0 || status=$?
          if [ "$status" -ne 0 ]; then
            echo "gofmt failed to run (exit $status):"
            echo "$out"
            exit "$status"
          fi
          unformatted=$(printf '%s\n' "$out" | grep -vE '^vendor/' || true)
          if [ -n "$unformatted" ]; then
            echo "The following files are not gofmt-formatted:"
            echo "$unformatted"
            exit 1
          fi
          echo "gofmt check passed."
