# IEEE 2030.5 (SEP2) reference

This server implements the IEEE 2030.5-2018 Smart Energy Profile (SEP2). It
ships a protocol listener for device traffic and an admin listener for the
operator surface; this document covers the protocol side. For the admin
surface see [`admin.md`](admin.md). For the CSIP V1.2 profile layered on
top, see [`csip.md`](csip.md).

The protocol router and route registrations live in
[`internal/server/router.go`](../internal/server/router.go). Handlers live
in [`internal/handler/`](../internal/handler/). Public Go types for every
resource live in [`pkg/sep2`](../pkg/sep2/).

## Function-set support

Status vocabulary:

- **Supported** - wire-conformant: routes registered, handler returns the
  spec resource, persistence wired through `pkg/store`.
- **Partial** - at least one route is wired but a non-trivial slice of the
  function set is missing (deferred sub-resources, mode coverage gap,
  no admin visibility, etc.). The "Notes" column names what is missing.
- **Planned** - not implemented; tracked. Cross-references the open ticket
  if one exists.
- **Not Started** - required by spec but no code yet.

| Function set | Status | Routes | Handler | Notes |
|---|---|---|---|---|
| DeviceCapability (DCAP) | Supported | `GET /dcap` | [`handler/dcap.go`](../internal/handler/dcap.go) | Mandatory entry point. Static link bundle; element order matches XSD. |
| Time | Supported | `GET /tm` | [`handler/time.go`](../internal/handler/time.go) | Wall clock with TZ + DST + quality fields. The `csip_test_hooks` build tag adds a test-controlled offset (CORE-006 / #27). |
| SelfDevice (SDEV) | Supported | `GET /sdev`, `GET /sdev/sdi` | [`handler/sdev.go`](../internal/handler/sdev.go), [`handler/device_info.go`](../internal/handler/device_info.go) | Server's own SFDI/LFDI plus DeviceInformation singleton. |
| EndDevice (EDEV) | Supported | `GET/POST /edev`, `GET/PUT/DELETE /edev/{id}` | [`handler/edev.go`](../internal/handler/edev.go) | Full CRUD; DELETE fans out a subscription Removed Notification (CSIP V1.2 MAINT-002 / #168). |
| Registration | Supported | `GET /edev/{id}/rg` | [`handler/registration.go`](../internal/handler/registration.go) | Device-side read; admin-side creation goes through `POST /api/devices` (see [`admin.md`](admin.md), #159 / #170). |
| FunctionSetAssignments (FSA) | Supported | `GET /edev/{id}/fsa`, `GET /edev/{id}/fsa/{fsaId}` | [`handler/fsa.go`](../internal/handler/fsa.go) | Spec-facing scoped surface. The admin-authored FSA-template management plane is separate (`internal/handler/admin_fsa.go`, #163). |
| DER | Supported | `GET /edev/{id}/der`, singletons `dercap`/`derg`/`ders`/`dera` (GET+PUT), `GET /edev/{id}/fsa/{fsaId}/derp`, `GET /edev/{id}/fsa/{fsaId}/derp/{derpId}/derc`, `GET/PUT .../dderc`, `GET /dc` | [`handler/der.go`](../internal/handler/der.go) | DER, DERCapability, DERSettings, DERStatus, DERAvailability, DERProgram, DERControl, DefaultDERControl, DERCurve. Mode-field gap on V1.2 ride-through / Volt-Watt / Freq-Watt / setGradW tracked under [#140](https://github.com/GRIDAPPSD/ieee-2030_5-go/issues/140). |
| Metering (UsagePoint) | Supported | `GET/POST /upt`, `GET /upt/{uptId}`, `GET /upt/{uptId}/mr`, `GET /upt/{uptId}/mr/{mrId}/r`, `GET /rt`, `GET /rt/{id}` | [`handler/metering.go`](../internal/handler/metering.go) | Server-side metering: UsagePoint, MeterReading, Reading, ReadingType. |
| Mirroring | Supported | `GET/POST /mup`, `GET /mup/{id}`, `POST /mup/{id}/mr` | [`handler/mirror.go`](../internal/handler/mirror.go) | Mirror UsagePoint and MirrorMeterReading post path used by aggregators. |
| Subscription / Notification | Supported | `GET/POST /edev/{id}/sub`, `DELETE /edev/{id}/sub/{subId}` | [`handler/subscription.go`](../internal/handler/subscription.go), [`internal/subscription/`](../internal/subscription/) | Per-EndDevice scoped (#168). DELETE fires a final Removed notification to the just-deleted subscriber (#169, CSIP V1.2 section 11.6). Notifier is router-injected. |
| Configuration | Supported | `GET/PUT /edev/{id}/cfg` | [`handler/configuration.go`](../internal/handler/configuration.go) | Singleton per EndDevice. |
| DeviceStatus | Supported | `GET/PUT /edev/{id}/dstat` | [`router.go`](../internal/server/router.go) | Singleton per EndDevice; wired via the generic `HandleSingletonGetPut` helper rather than its own handler package. |
| LogEvent | Supported | `GET/POST /edev/{id}/log` | [`handler/log_event.go`](../internal/handler/log_event.go) | LogEvent list scoped per device. |
| PowerStatus | Supported | `GET/PUT /edev/{id}/ps` | [`handler/power_status.go`](../internal/handler/power_status.go) | Singleton per EndDevice. |
| Messaging | Supported | `GET /msg`, `GET /msg/{msgId}`, `GET/POST /msg/{msgId}/tm` | [`handler/messaging.go`](../internal/handler/messaging.go) | MessagingProgram + TextMessage. |
| FlowReservation | Supported | `GET/POST /edev/{id}/frq`, `GET /edev/{id}/frp` | [`handler/flow_reservation.go`](../internal/handler/flow_reservation.go) | Request and Response. The 5xx-on-store-failure path is regression-tested under #4. |
| Response | Supported | `GET /rsps`, `GET/POST /rsps/{rspsId}/rsp` | [`handler/messaging.go`](../internal/handler/messaging.go) | ResponseSet + Response. The list GET keys on `rspsId` (not the generic `id` placeholder) per #61. Bare-error response on store failure tracked under [#243](https://github.com/GRIDAPPSD/ieee-2030_5-go/issues/243). |
| DRLC (Demand Response / Load Control) | Planned | - | - | `FunctionSetDRLC` is defined as a LogEvent function-set ID in [`pkg/sep2/log_event.go`](../pkg/sep2/log_event.go) but no handler or routes are wired. No open ticket yet; file one if a deployment needs it. |
| EXI encoding | Removed | - | - | EXI codec was removed in #2. XML is the only supported wire encoding ([`internal/encoding/encoder.go`](../internal/encoding/encoder.go)). |

## Transport

The protocol listener binds `SEP2_ADDR` (default `:443`) and requires
mutual TLS. Every request walks
[`auth.IdentityMiddleware`](../internal/auth/identity.go) (extracts
SFDI/LFDI from the peer cert) and
[`auth.ACLMiddleware`](../internal/auth/acl.go) (per-prefix method/auth
bitmap, longest-prefix match).

The default ACL - `auth.DefaultACLRules()` in `acl.go` - covers `/dcap`,
`/tm`, `/sdev`, `/edev`, `/mup`, `/dc`, `/upt`, `/rt`, `/msg`, `/rsps`.
`/dcap` and `/tm` allow `AuthNone | AuthDeviceCert`; everything else
requires `AuthDeviceCert`.

Two TLS modes are wired:

- **[GCM](glossary.md)** (`make run`) - Go stdlib `crypto/tls`. Not
  spec-compliant; fine for local development.
- **[CCM-8](glossary.md)** (`make run-ccm`, `make run-full`, `SEP2_CCM=true`) - vendored
  `internal/tls/gotls` fork registers `TLS_ECDHE_ECDSA_WITH_AES_128_CCM_8`
  (code point `0xc0ae`). The cipher list is CCM-8 first, GCM as a
  fallback. See [`csip.md`](csip.md) for the spec context and the
  fallback caveat.

## Wire encoding

XML only (`application/xml`). The
[`encoding.NamespaceMiddleware`](../internal/encoding/namespace.go) wraps
the entire router and rewrites the namespace declaration on the way out
for legacy 2013 clients (the EPRI reference client lives in this group).

## Discovery

mDNS advertisement is opt-in via `SEP2_MDNS=true`
([`internal/discovery/`](../internal/discovery/)). Service type
`_smartenergy._tcp` advertises the bound `SEP2_ADDR` under the
`SEP2_MDNS_HOST` name.

## Persistence

`SEP2_DATA_DIR` is the single knob (#165). Empty keeps the
historical pure in-memory behavior; non-empty turns on disk-backed
EndDevice, Registration, DERProgram, Subscription, and AdminFSA
(#163) stores. The deprecated `SEP2_SUBSCRIPTION_STORE_PATH` is
preserved as an override when both are set.

## Tests

| Target | Scope |
|---|---|
| `make test` | Full Go test suite. |
| `make test-race` | Race detector across the suite. |
| `make test-cover` | Coverage profile + HTML report. |
| `make test-csip` | CSIP V1.2 conformance harness under the production build tag. |
| `make test-csip-hooks` | Same harness with the `csip_test_hooks` build tag (mutation + time-advance hooks compiled in, #27 / #28). |
| `make test-csip-cover` | CSIP-scoped coverage profile (gate at 80% via `make coverage-gate`, #193 / #212). |
| `make test-epri` | EPRI C client interop against a running CCM-8 server (`scripts/test-epri-client.sh`). |
| `make test-e2e` | Playwright dashboard suite under [`e2e/`](../e2e/). |

## Cross-references

- [README](../README.md)
- [`csip.md`](csip.md) - CSIP V1.2 profile
- [`admin.md`](admin.md) - operator surface
- [`admin-listener.md`](admin-listener.md) - admin listener TLS posture
- [`glossary.md`](glossary.md) - acronyms and protocol terms
